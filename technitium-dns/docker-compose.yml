services:
  dns-server:
    container_name: dns-server
    hostname: dns-server
    image: technitium/dns-server:latest
    network_mode: "host"
    env_file: docker-compose.env
    volumes:
      - ./config:/etc/dns
    restart: always
